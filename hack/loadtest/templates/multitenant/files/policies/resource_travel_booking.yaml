---
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  resource: travel_booking
  version: default
  rules:
    - actions: ["create", "view", "cancel", "approve"]
      effect: EFFECT_ALLOW
      roles: ["admin", "jr_admin", "assistant", "manager"]
      condition:
        match:
          expr: R.attr.tenantId == P.attr.tenantId
