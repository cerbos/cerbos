---
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  resource: training_record
  version: default
  rules:
    - actions: ["create", "view", "complete", "delete"]
      effect: EFFECT_ALLOW
      roles: ["admin", "jr_admin", "assistant", "manager"]
      condition:
        match:
          expr: R.attr.tenantId == P.attr.tenantId
