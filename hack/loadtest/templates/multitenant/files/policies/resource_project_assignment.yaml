---
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  resource: project_assignment
  version: default
  rules:
    - actions: ["create", "view", "reassign", "close"]
      effect: EFFECT_ALLOW
      roles: ["admin", "jr_admin", "assistant", "manager"]
      condition:
        match:
          expr: R.attr.tenantId == P.attr.tenantId
