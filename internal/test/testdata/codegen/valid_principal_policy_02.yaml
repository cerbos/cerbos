---
inputPolicy:
  apiVersion: "cerbos.dev/v1"
  principalPolicy:
    principal: daffy_duck
    version: "20210210"
    rules:
      - resource: "*"
        actions:
          - action: "*"
            condition:
              match:
                expr:
                  - "request.resource.attr.dev_record == true"
            effect: EFFECT_ALLOW

wantRego: |-
  package cerbos.principal.daffy_duck.v20210210

  default effect = "no_match"

  effect = "allow" {
      glob.match("*", [], input.resource.name)
      glob.match("*", [], input.action)
      cel_eval(input, "cerbos.principal.daffy_duck.v20210210", "cond_0")
  }

wantNumConditions: 1
