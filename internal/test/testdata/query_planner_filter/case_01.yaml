# yaml-language-server: $schema=../.jsonschema/QueryPlannerFilterTestCase.schema.json
---
description: AND all true cases
input:
  kind: KIND_CONDITIONAL
  condition:
    expression:
      operator: and
      operands:
        - value: true
        - value: true
        - value: true
wantFilter:
  kind: KIND_ALWAYS_ALLOWED
wantString: "(true)"
