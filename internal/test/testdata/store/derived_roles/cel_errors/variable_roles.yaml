# yaml-language-server: $schema=../../../../../../schema/jsonschema/cerbos/policy/v1/Policy.schema.json
---
apiVersion: api.cerbos.dev/v1
derivedRoles:
  name: cel_errors.variable_roles
  variables:
    local:
      dv: R.attr.amount > 1000
  definitions:
    - name: spender
      parentRoles:
        - user
      condition:
        match:
          expr: V.dv
