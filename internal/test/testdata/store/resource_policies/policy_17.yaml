---
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  resource: calendar_entry
  version: default
  rules:
    - actions:
        - nonaction
        - plan
      effect: EFFECT_ALLOW
      roles:
        - employee
