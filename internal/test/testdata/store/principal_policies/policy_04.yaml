---
apiVersion: "api.cerbos.dev/v1"
principalPolicy:
  principal: terry_tibbs
  version: "default"
  rules:
    - resource: equipment_request
      actions:
        - action: "create"
          effect: EFFECT_ALLOW
          output:
            expr: |-
              'create_override_for_terry_tibbs_talk_to_me'
