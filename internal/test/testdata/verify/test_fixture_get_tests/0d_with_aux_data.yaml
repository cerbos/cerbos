table:
  name: Zero-dimensional table test
  description: A test for a single principal and resource, with aux data.
  input:
    principal: employee
    resource: employee_leave_request
    actions:
      - view
      - approve
    auxData: test_aux_data
  expected:
    - actions:
        view: EFFECT_ALLOW
        approve: EFFECT_DENY

wantTests:
  - name:
      testTableName: Zero-dimensional table test
      principalKey: employee
      resourceKey: employee_leave_request
    description: A test for a single principal and resource, with aux data.
    input:
      principal:
        id: user:employee
      resource:
        id: leave_request:employee
      actions:
        - view
        - approve
      auxData:
        jwt:
          answer: 42
    expected:
      view: EFFECT_ALLOW
      approve: EFFECT_DENY
