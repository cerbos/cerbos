table:
  name: No resources
  description: Either resource or resources is required in input.
  input:
    principal: employee
    actions:
      - view
      - approve
  expected:
    - actions:
        view: EFFECT_ALLOW
        approve: EFFECT_DENY

wantErr: |-
  invalid test "No resources": wanted one of "resource" or "resources" in input; got neither
