table:
  name: Principal and principals
  description: You can't specify both principal and principals in input.
  input:
    principal: employee
    principals:
      - manager
    resource: employee_leave_request
    actions:
      - view
      - approve
  expected:
    - actions:
        view: EFFECT_ALLOW
        approve: EFFECT_DENY

wantErr: |-
  invalid test "Principal and principals": wanted one of "principal" or "principals" in input; got both
