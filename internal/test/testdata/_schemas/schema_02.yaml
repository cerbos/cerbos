---
apiVersion: "api.cerbos.dev/v1"
description: |-
  Schema definition file used in tests

principalSchema: # define schema for request.principal.attr
  type: object
  properties:
    list:
      type: array
      description: "a list"
    objectsInObject:
      type: object
      description: "objects in object example"
      properties:
        obj1:
          type: object
          description: "object 1"
          properties:
            somebool:
              type: boolean
              description: "a boolean"
            somenumber:
              type: number
              description: "an integer"
            somefloat:
              type: number
              description: "a float"
        obj2:
          type: object
          description: "object 2"
          properties:
            somebool:
              type: boolean
              description: "a boolean"
            somenumber:
              type: number
              description: "an integer"
            somefloat:
              type: number
              description: "a float"
            somelist:
              type: array
              description: "some list"

resourceSchemas: # define schema for request.resource.attr
  leave_request:
    type: object
    properties:
      department:
        type: string
      geography:
        type: string
      team:
        type: string
      id:
        type: string
      owner:
        type: string