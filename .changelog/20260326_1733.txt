type:fix
Fix issues with the directory watch system
Fixes an issue where renaming a policy file causes it to be dropped from the index. Also addresses an edge case where change detection fails if the inode of the initial directory changes while the watch is active.
